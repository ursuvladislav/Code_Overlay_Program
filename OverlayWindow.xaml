<Window x:Class="CodeOverlayRunner.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="150" Height="90">
    <Border CornerRadius="14"
            Background="#CC4A4A4A"
            Padding="12">
        <TextBlock x:Name="TextOut"
                   Foreground="White"
                   FontSize="12"
                   TextWrapping="Wrap"/>
    </Border>
</Window>